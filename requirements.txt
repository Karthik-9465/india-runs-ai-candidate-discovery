# ============================================================================
# Redrob Hackathon: Intelligent Candidate Discovery & Ranking System
# ============================================================================

# 1. CORE PIPELINE (rank.py & src/ modules)
# The core ranking engine has ZERO external dependencies.
# It runs entirely on the Python Standard Library (no numpy, pandas, or scikit-learn).
# This guarantees maximum execution speed and zero compatibility issues in the
# Stage 3 Docker reproduction container.

# 2. STREAMLIT INTERACTIVE DASHBOARD (app.py) & SANDBOX DEMO
# These dependencies are required only if you want to run the visual interface
# or deploy the sandbox/demo link (Streamlit Cloud or HuggingFace Spaces).
streamlit>=1.20.0
pandas>=1.3.0
